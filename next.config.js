/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  env: {
    CONNECTION_STRING: 'Neon Connection Successfully Connected Yahoo! 👻💩',

  }
}

module.exports = nextConfig